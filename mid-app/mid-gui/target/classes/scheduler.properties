# scheduler.properties
quartz.enabled=true
quartz.schedule.type=simple  # simple or cron
quartz.simple.interval.minutes=2
quartz.cron.expression=0 */2 * * * ?
quartz.thread.pool.size=3
quartz.job.description=Import policies created today

# Logging
quartz.log.enabled=true
quartz.log.level=INFO